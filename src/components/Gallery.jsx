import React, { useState } from 'react';
import { Routes } from "react-router";
import Nav from './Nav';

const Gallery = () => {
        return (
            <div>
                <Nav/>
            </div>
        )
}

export default Gallery;